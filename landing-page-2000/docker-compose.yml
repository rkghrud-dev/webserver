services:
  landing-page-2000:
    build: .
    container_name: landing-page-2000
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "2000:8000"
    volumes:
      - ./data:/data
