# RK Work Portal 환경변수 (서버에서는 .env 로 복사해 채운다. .env 는 git/배포 로그에 노출 금지)
# 첫 기동 시 이 관리자 계정이 자동 생성된다(status=active, 관리자 권한).
PORTAL_ADMIN_EMAIL=admin@rk.local
PORTAL_ADMIN_PASSWORD=change-me-strong-password

# SQLite 위치(컨테이너 내부). docker-compose 의 ./data 볼륨에 영구 저장된다.
PORTAL_DB=/data/portal.db
