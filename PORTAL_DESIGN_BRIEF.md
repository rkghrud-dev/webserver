# RK Work Portal Design Brief

## 목표

현재 서버 포털을 단순 랜딩페이지가 아니라, 로그인 후 업무별 프로그램으로 들어가는 작업 홈으로 만든다.

관리자도 로그인하면 바로 회원승인관리 화면으로 보내지 않고, 일반 사용자와 같은 기본 포털 홈으로 들어간다. 대신 관리자에게만 상단 메뉴에서 `회원 승인 관리`를 열 수 있는 메뉴를 보여준다.

## 기본 진입 흐름

1. 사용자는 `http://141.164.40.19:2000`에 접속한다.
2. 첫 화면에는 로그인 카드와 회원가입 버튼이 보인다.
3. 로그인 후 기본 포털 홈으로 이동한다.
4. 포털 홈에는 주요 업무 카드가 보인다.
5. 관리자 계정으로 로그인한 경우 상단 메뉴에 관리자 전용 메뉴가 추가된다.

## 로그인 후 기본 포털 홈

기본 홈은 모든 사용자와 관리자에게 동일한 느낌으로 보여준다.

홈 화면 주요 카드:

- `WebOCR`
- `CafeShipment`
- `키 설정 / 키 생성`
- 추후 추가될 업무 페이지 카드

관리자는 이 홈 화면 위쪽 메뉴에서 `회원 승인 관리`를 열 수 있다.

## 관리자 화면 구조

관리자 로그인 후 바로 `회원 승인 관리`로 보내지 않는다.

관리자 로그인 후 화면:

- 기본 포털 홈
- 상단 우측 관리자 메뉴
- 관리자 메뉴 안에:
  - 회원 승인 관리
  - 회원별 권한 설정
  - 다운로드 권한 설정
  - 추후 관리자 기능

표현 방식:

- 상단 우측에 `관리자` 버튼
- 클릭 시 작은 드롭다운 또는 팝업 메뉴
- 메뉴에서 `회원 승인 관리` 선택
- 선택하면 승인 관리 페이지 또는 패널로 이동

## WebOCR 카드

역할:

- WebOCR 프로그램 다운로드 페이지로 이동한다.
- 승인된 회원만 실제 다운로드 버튼을 볼 수 있다.
- 승인 대기 회원은 설명과 승인 대기 상태만 본다.

카드 안에 들어갈 내용:

- 제목: `WebOCR`
- 설명: 상품 이미지와 마켓 정보를 처리하는 OCR 기반 업무 프로그램
- 상태:
  - 승인됨: `다운로드 가능`
  - 승인 대기: `관리자 승인 후 다운로드 가능`
  - 비활성: `사용 중지`
- 버튼:
  - `다운로드 페이지`
  - 승인된 경우 `WebOCR EXE 다운로드`

다운로드 페이지 구성:

- 프로그램 설명
- 현재 최신 버전
- 파일 크기
- 업데이트 방식 안내
- 다운로드 버튼
- 실행 후 로그인/자동 업데이트 안내

## CafeShipment 카드

역할:

- CafeShipment 업무 화면으로 이동한다.
- 첫 구현에서는 바로 `주문 수집`을 할 수 있는 화면을 중심으로 만든다.

카드 안에 들어갈 내용:

- 제목: `CafeShipment`
- 설명: Cafe24 주문/배송 정보를 수집하고 출고 작업을 준비하는 프로그램
- 주요 버튼:
  - `주문 수집 시작`
  - `수집 내역 보기`
  - `배송 설정`

CafeShipment 페이지 구성:

- 상단: 연결된 마켓 선택
- 중앙: `주문 수집` 버튼
- 하단:
  - 최근 수집 시간
  - 수집된 주문 수
  - 실패/오류 상태
  - 로그 요약

초기 화면은 실제 기능이 완성되기 전이라도 버튼과 상태 영역을 먼저 잡아둔다.

## 키 설정 / 키 생성 카드

역할:

- 마켓별 API 키를 서버에 저장하는 기존 키 설정 화면으로 연결한다.
- 회원이 왜 키를 넣어야 하는지 설명한다.

카드 안에 들어갈 내용:

- 제목: `키 설정`
- 설명: 쿠팡, Cafe24, 기타 마켓 API 키를 서버에 안전하게 저장한다.
- 상태:
  - 키 미등록
  - 일부 등록
  - 등록 완료
- 버튼:
  - `키 설정 열기`
  - `마켓 추가`

설명 칸 내용:

- 키는 개인 PC가 아니라 서버에 저장된다.
- 관리자 승인 후 저장할 수 있다.
- 저장된 키는 WebOCR, CafeShipment 같은 프로그램에서 사용된다.
- Access Key / Secret Key 같은 민감 정보는 화면에 다시 노출하지 않는다.

## 추후 확장 구조

포털 홈은 카드형 업무 런처로 유지한다.

추후 추가 가능한 카드:

- 게시판
- 파일 업로드
- 상품 등록 자동화
- 주문 대시보드
- 배송 대시보드
- 관리자 통계
- 고객사별 권한 관리

확장 원칙:

- 새 기능은 홈 카드 하나로 추가한다.
- 관리자 전용 기능은 상단 관리자 메뉴 안에 넣는다.
- 회원이 자주 쓰는 기능은 홈 카드로 노출한다.
- 승인/권한이 필요한 기능은 카드 안에 상태 배지를 표시한다.

## 권한 상태

회원과 마켓은 아래 상태를 가진다.

- `승인 대기`
- `승인됨`
- `비활성`

상태별 동작:

- 승인 대기:
  - 포털 홈 접근 가능
  - 설명 페이지 접근 가능
  - 다운로드/키 저장/주문 수집은 제한
- 승인됨:
  - 다운로드 가능
  - 키 저장 가능
  - 주문 수집 가능
- 비활성:
  - 홈 접근은 가능하되 주요 실행 버튼 제한

## 화면 분위기

디자인 방향:

- 업무용 SaaS 느낌
- 깔끔한 관리자 포털
- 너무 화려한 랜딩페이지가 아니라, 실제 작업자가 매일 들어와도 피곤하지 않은 화면
- 카드, 테이블, 상태 배지 중심
- 흰색/연한 회색 바탕
- 파란색을 주요 액션 색상으로 사용
- 승인/성공은 초록색
- 대기/주의는 노란색
- 비활성/오류는 빨간색

레이아웃:

- 상단 고정 헤더
- 좌측 또는 중앙에 서비스 카드
- 우측 상단 사용자/관리자 메뉴
- 모바일에서는 카드가 세로로 쌓임

## 클로드 디자인용 프롬프트

아래 내용을 클로드에 붙여서 먼저 예쁜 시안으로 그려볼 수 있다.

```text
한국어 업무용 SaaS 포털 UI를 디자인해줘.

서비스 이름은 "RK Work Portal"이고, 사용자는 로그인 후 WebOCR, CafeShipment, 키 설정 페이지로 들어간다.

중요한 구조:
- 첫 화면은 로그인 화면
- 로그인 후 기본 포털 홈이 열린다
- 관리자도 로그인 후 바로 회원승인관리로 가지 않고 기본 포털 홈으로 들어간다
- 관리자에게만 상단 우측 "관리자" 드롭다운 메뉴가 보인다
- 관리자 메뉴 안에는 "회원 승인 관리", "회원별 권한 설정", "다운로드 권한 설정"이 있다

포털 홈 카드:
1. WebOCR
   - 프로그램 다운로드 페이지로 이동
   - 승인된 회원만 EXE 다운로드 가능
   - 승인 대기 회원은 설명과 승인 대기 상태만 보임

2. CafeShipment
   - 주문 수집 업무 화면
   - "주문 수집 시작", "수집 내역 보기", "배송 설정" 버튼
   - 최근 수집 시간, 수집된 주문 수, 실패 상태를 보여주는 영역

3. 키 설정
   - 마켓별 API 키를 서버에 저장하는 페이지
   - 쿠팡, Cafe24, 기타 마켓 키 저장 설명
   - "키 설정 열기", "마켓 추가" 버튼

디자인 스타일:
- 깔끔한 업무용 SaaS 대시보드
- 흰색/연회색 배경
- 파란색 주요 버튼
- 초록색 승인 상태
- 노란색 승인 대기 상태
- 빨간색 비활성/오류 상태
- 카드 radius는 8px 이하
- 상단 헤더, 상태 배지, 테이블, 드롭다운 메뉴 포함
- 마케팅 랜딩페이지처럼 만들지 말고 실제 업무자가 매일 쓰는 포털처럼 만들어줘
- 한국어 텍스트가 버튼과 카드 안에서 넘치지 않게 해줘

먼저 데스크톱 화면 시안을 만들고, 이어서 모바일에서 카드가 세로로 쌓이는 반응형 구조도 설명해줘.
```

## 구현 메모

현재 서버 기준으로는 `coupang-wing-api-4000` 앱에 이미 회원/마켓 DB와 관리자 승인 로직이 있다.

다음 구현 방향:

1. 관리자 로그인 후 `admin_page`로 바로 보내는 흐름을 `portal_dashboard`로 변경한다.
2. 관리자 여부를 포털에 전달한다.
3. 관리자일 때만 상단 메뉴에 `회원 승인 관리`를 노출한다.
4. `WebOCR` 페이지는 다운로드 설명 페이지로 만든다.
5. `CafeShipment` 페이지는 주문 수집 중심 화면으로 만든다.
6. `키 설정`은 기존 `/keys/login`, `/keys/manage` 흐름을 유지하면서 포털 카드에서 연결한다.
7. 추후 EXE 다운로드와 자동 업데이트 API를 붙인다.
