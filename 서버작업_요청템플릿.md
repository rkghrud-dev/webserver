# 서버 작업 요청 템플릿

아래 내용을 Codex에게 붙여넣고 원하는 부분만 바꿔서 요청하면 됩니다.

---

## 기본 서버 정보

- 서버 IP: `141.164.40.19`
- 서버 접속 명령어: `ssh -i ~/.ssh/vultr_api_gateway_deploy root@141.164.40.19`
- 서버 OS: Ubuntu 24.04
- 서버 위치: Vultr Seoul
- 기본 API 게이트웨이 상태 확인 주소: `http://141.164.40.19/health`
- API 키 확인 주소: `http://141.164.40.19/api-key`
- 현재 80번 포트는 기본 API 게이트웨이가 사용 중입니다.

## 내가 원하는 작업

아래 중 하나처럼 요청하면 됩니다.

예시:

```text
2000번 포트에 랜딩페이지 만들어줘.
```

```text
3000번 포트에 게시판 만들어줘.
```

```text
4000번 포트에 쿠팡 Wing API 연결 서버 만들어줘.
```

---

## 이번에 만들 것

- 사용할 포트:
  - 예: `2000`

- 만들 기능:
  - 예: 랜딩페이지 / 게시판 / 파일 업로드 / 쿠팡 API 연동 / 관리자 페이지

- 원하는 화면:
  - 예: 깔끔한 SaaS 느낌
  - 예: 모바일에서 보기 편하게
  - 예: 상품 소개 페이지처럼
  - 예: 글 목록, 글 작성, 글 상세가 있는 게시판

- 필요한 내용:
  - 예: 제목, 설명, 버튼, 이미지, 입력폼, 상품명, 문의 버튼 등

- 중요한 주의사항:
  - 기존 80번 API 게이트웨이는 건드리지 말 것
  - 새 기능은 새 폴더에 만들 것
  - API 키나 비밀번호는 내 PC가 아니라 서버 안 `.env`에만 저장할 것
  - 프로그램이 API 키를 직접 들고 있지 않게 가능하면 서버가 대신 API를 호출하게 만들 것
  - 작업 후 접속 주소를 알려줄 것

---

## Codex에게 이렇게 요청하기

아래 문장을 복사해서 Codex에게 붙여넣고, 원하는 부분만 바꾸면 됩니다.

```text
나는 서버/도커를 잘 모릅니다. 쉽게 설명해 주세요.

서버 정보:
- IP: 141.164.40.19
- SSH: ssh -i ~/.ssh/vultr_api_gateway_deploy root@141.164.40.19
- OS: Ubuntu 24.04
- 기본 health: http://141.164.40.19/health
- API 키 endpoint: http://141.164.40.19/api-key

요청:
- [여기에 포트 번호]번 포트에 [만들 기능]을 만들어 주세요.
- 기존 80번 api-gateway는 건드리지 마세요.
- 새 폴더를 /opt 아래에 만들고 Docker로 실행해 주세요.
- 필요한 방화벽 포트도 열어 주세요.
- API 키, 비밀번호, 토큰은 서버의 .env에 저장하고 화면이나 로그에 출력하지 마세요.
- 작업 후 접속 주소와 확인 방법을 알려 주세요.
- 제가 복사해서 붙여넣을 수 있게 명령어는 한 줄씩 안내해 주세요.

원하는 내용:
- [여기에 원하는 설명을 적기]
```

---

## 포트 예시

- `1000`: 파일 업로드 서버
- `2000`: 랜딩페이지
- `3000`: 게시판
- `4000`: 쿠팡 Wing API 서버
- `5000`: 관리자 페이지

꼭 이 번호를 지켜야 하는 건 아닙니다. 겹치지만 않으면 됩니다.

---

## 내가 이해하면 되는 핵심

- 포트는 서버 안의 방 번호 같은 것입니다.
- `141.164.40.19:2000`은 2000번 방입니다.
- `141.164.40.19:3000`은 3000번 방입니다.
- 각 방마다 다른 사이트나 프로그램을 띄울 수 있습니다.
- API 키는 내 컴퓨터로 가져오는 게 아니라 서버 안에 숨겨두고 서버가 대신 사용합니다.

---

## 서버 상태 확인

바탕화면의 `server-status.bat`를 실행하면 서버 상태를 확인할 수 있습니다.

정상이라면 이런 내용이 보입니다.

```text
Ping 성공
Health OK: {"ok":true}
```

이 두 개가 보이면 서버는 살아있는 상태입니다.

---

## API 키 서버 사용

현재 API 게이트웨이는 서버 `.env`에 저장된 API 키 정보를 `X-Client-Token`이 맞을 때만 내려줍니다.

```bash
curl -H "X-Client-Token: YOUR_CLIENT_TOKEN" http://141.164.40.19/api-key
```

중요:

- `YOUR_CLIENT_TOKEN`은 서버 `.env`의 `CLIENT_TOKEN` 값입니다.
- 실제 API 키는 대화창, 로그, 스크린샷에 그대로 출력하지 않는 것이 좋습니다.
- 더 안전한 방식은 프로그램이 API 키를 받는 방식이 아니라, 서버가 외부 API를 대신 호출해 주는 방식입니다.
